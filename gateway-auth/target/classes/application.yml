server:
  port: 8888
spring:
  application:
    name: gateway-auth
  cloud:
    nacos:
      discovery:
        server-addr: 47.95.232.196:8847
        namespace: public

    gateway:
      discovery:
        locator:
          enabled: true
      enabled: true
      routes:
      - id: exam-auth
        uri: lb://exam-auth
        predicates:
        - Path=/oauth/**,/user/**
#      - id: order-servicess
#        uri: lb://order-servicess
#        predicates:
#        - Path=/order/**

exam:
  gateway:
    shouldSkipUrls:
    - /oauth/**
    - /sso/**